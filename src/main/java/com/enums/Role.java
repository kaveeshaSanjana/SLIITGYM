package com.enums;

/**
 * Role enum - Defines the three types of users in the gym system.
 * ADMIN can manage everything, INSTRUCTOR teaches classes, MEMBER uses the gym.
 */
public enum Role {
    ADMIN,
    INSTRUCTOR,
    MEMBER
}


