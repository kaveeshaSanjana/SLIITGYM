package com.enums;

/**
 * PaymentStatus enum - Current state of a payment.
 * PAID = completed, PENDING = awaiting payment, OVERDUE = past due date.
 */
public enum PaymentStatus {
    PAID,
    PENDING,
    OVERDUE
}


